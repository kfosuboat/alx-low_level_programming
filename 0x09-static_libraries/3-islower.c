#include "main.h"

/**
 * _islower - checks for lowercase characters.
 * @c: the character to check.
 *
 * Return: 1 if c is a lowercase letter, 0 not.
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
